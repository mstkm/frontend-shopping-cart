import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Product",
    description: "Product Shopping Cart",
};

const ProductPage = () => {
    return (
        <div>
            <h1>Ini halaman produks untuk customer</h1>
        </div>
    )
}

export default ProductPage;